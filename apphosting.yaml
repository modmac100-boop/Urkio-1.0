runConfig:
  runtime: nodejs20
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 10

# Set the root directory so App Hosting uses the Expo app's package.json
rootDirectory: Urkio-Mobile

outputFiles:
  serverOutputDirectory: dist
